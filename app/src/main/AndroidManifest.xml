<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- The camera is optional: the rest of the app works without it. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_BLOOD_GLUCOSE" />
    <uses-permission android:name="android.permission.health.READ_NUTRITION" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_MENSTRUATION" />
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_HISTORY" />
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_IN_BACKGROUND" />
    <uses-permission android:name="android.permission.health.WRITE_BLOOD_GLUCOSE" />
    <uses-permission android:name="android.permission.health.WRITE_NUTRITION" />
    <uses-permission android:name="android.permission.health.WRITE_EXERCISE" />

    <!-- FEATURE: cgm-direct-ingest
         xDrip+ sendBroadcast(intent, RECEIVE_BG_ESTIMATE) only delivers to
         apps that hold this permission, unless the user enables xDrip's
         "without permission" switch. -->
    <uses-permission android:name="com.eveningoutpost.dexdrip.permissions.RECEIVE_BG_ESTIMATE" />

    <!-- Declared for a future sender that wants to opt in. Not attached to
         the receiver: xDrip+ and AAPS do not request it. Sender checks use
         the package allowlist in XdripBroadcastAllowlist. -->
    <permission
        android:name="com.omb9.glucosehero.permission.SEND_XDRIP_CGM"
        android:description="@string/xdrip_permission_description"
        android:label="@string/xdrip_permission_label"
        android:protectionLevel="normal" />

    <!-- Health Connect is a separate installable app below Android 14; query
         its package so availability checks and permission-usage intents can
         resolve it. -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
        <!-- FEATURE: cgm-direct-ingest — resolve xDrip+ / AAPS for the sender allowlist. -->
        <package android:name="com.eveningoutpost.dexdrip" />
        <package android:name="info.nightscout.androidaps" />
        <package android:name="info.nightscout.aapsclient" />
        <package android:name="info.nightscout.aapsclient2" />
        <package android:name="info.nightscout.aapspumpcontrol" />
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
        </intent>
    </queries>

    <application
        android:name=".GlucoseHeroApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ghlogo"
        android:roundIcon="@mipmap/ghlogo_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GlucoseHero"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Automatically download the Play services code scanner module on app install -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode_ui" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.GlucoseHero">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Pre-Android-14 rationale route. -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!-- TODO: Both rationale routes currently land on the app's normal
             start destination. A dedicated rationale screen is owed in a
             later wave. -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".crisis.HypoSosPromptActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.GlucoseHero" />

        <service
            android:name=".crisis.HypoSosForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Severe hypoglycemia caregiver SOS countdown" />
        </service>

        <!-- FEATURE: cgm-direct-ingest -->
        <service
            android:name=".data.cgm.nightscout.NightscoutForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Optional user-opt-in Nightscout CGM poll at a 5-minute cadence" />
        </service>

        <receiver
            android:name=".crisis.HypoSosAlarmReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ui.glance.GlucoseHeroGlanceWidgetReceiver"
            android:exported="true"
            android:label="@string/glucose_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/glucose_hero_widget_info" />
        </receiver>

        <!-- FEATURE: cgm-direct-ingest
             Manifest path for explicit xDrip+ Identify-receiver / AAPS
             queryBroadcastReceivers sends. Disabled until the user opt-in
             persists via PackageManager. Implicit custom-action broadcasts
             on API 26+ need the runtime twin in XdripBroadcastRuntime.
             No android:permission: xDrip+ and AAPS cannot hold SEND_XDRIP_CGM.
             XdripBroadcastAllowlist is the sender gate. -->
        <receiver
            android:name=".data.cgm.xdrip.XdripGlucoseBroadcastReceiver"
            android:enabled="false"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.eveningoutpost.dexdrip.BgEstimate" />
                <action android:name="info.nightscout.client.NEW_SGV" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- FEATURE: wear-os-companion -->
        <service
            android:name=".wear.WearMessageListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:host="*"
                    android:pathPrefix="/glucosehero"
                    android:scheme="wear" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

        <!-- Disable default WorkManager initializer; we provide a Hilt-aware
             Configuration from GlucoseHeroApp instead. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
